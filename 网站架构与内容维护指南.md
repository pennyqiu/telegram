# 网站架构与内容维护指南

> **一句话目的**：以后往两个已上线的网站加内容，直接照本文对应章节改**一个文件/放一个文件**即可，不需要重新生成整套网站。

本文覆盖两个对外网站：

| 网站 | 线上地址 | 定位 | 仓库入口 | 形态 |
|------|----------|------|----------|------|
| **美股系统学习** | `learn.tgfootclub.com` | 30+ 节课的美股学习路径（含登录付费墙） | `us-stock-learning.html` | **单一自包含 HTML**（SPA） |
| **香港保险综合站** | `insurance.tgfootclub.com` | 保险知识库 + 内容工厂 + 运营战略 | `insurance-guide/index.html` | **多页静态 HTML** 站点 |

> 另有一个内部用的「美股持仓复盘 · 文档中心」(`美股持仓复盘/站点/index.html`)，由 `美股持仓复盘/tools/build_site.py` 生成，属于第三类站点，本文末尾附带说明。

---

## 一、部署逻辑（两站通用，先看这个）★已核实

- **服务器**：VPS `202.182.104.186`（主机名 `tg-club-system`，Ubuntu），用 **nginx** 对外提供，SSL 由 Certbot 管理。
- **托管方式**：两个站都是**纯静态前端**，直接由 nginx 从**服务器上的 git 检出目录**读取文件对外提供。**服务器上有两个独立的 git 检出**，都是同一个仓库 `github.com/pennyqiu/telegram.git`（分支 `main`）：

| 站点（域名） | nginx `root` | 实际磁盘位置 | 首页 |
|------|------|------|------|
| **learn**.tgfootclub.com | `/app/telegram` | git 检出①：`/app/telegram` | `us-stock-learning.html` |
| **insurance**.tgfootclub.com | `/var/www/hkins-guide` | 软链 → git 检出②：`/var/www/telegram-repo/insurance-guide` | `index.html` |

- **前端 → 后端**全用相对路径同域反代：`learn` 的 `location /api/` 反代到 `http://127.0.0.1:8000`（就是 systemd 服务 **`price-api.service`**，负责登录鉴权 `/api/v1/course/auth`、投资大师动态等）。章节音频走 `location /api/v1/audio/` → 磁盘 `/app/telegram/audio/`（当前为空）。
- 其它同服务器上的兄弟域名（参考用，非本文重点）：`club`（后端代理）、`admin`→`/app/telegram/tg-club/admin-web/dist`、`mini`→`/var/www/mini-app`、`brief`→`/var/www/briefing`、`learn` 下还挂了 `/kol-radar/`（带 basic auth）。

**发布 = 本地 `git push`，就这一步，永不重建整站**。服务器两个检出都配了 cron **每 3 分钟自动 `git pull`**（见下方「自动部署」），所以推送后最多等 3 分钟自动上线，**不用登服务器**。想立刻生效再手动去服务器 `git pull`（附录 A）。纯静态改动**连 nginx 都不用重载**，浏览器强刷即可看到（HTML/CSS/JS 有 7 天缓存，见附录 A 的强刷说明）。只有改到登录后端逻辑时才需要 `systemctl restart price-api`。

> 具体命令见文末 **附录 A**（已填好实测命令）。

### ✅ 曾有的“脏工作区”已处理

之前 `/app/telegram` 的 `git status` 显示两处“改动”（`articles/_style.css`、`articles/easter-egg-cpa-cross-border-tax.html`）。经 `git diff` 核实，**内容完全没变，只是文件权限位从 `100644` 变成了 `100755`**（被加了可执行位），不是真实改动。已在两个检出上执行 `git config core.fileMode false`，让 git **忽略权限位差异**，工作区恢复干净，`git pull` 不再受影响。

> **规矩**：今后统一“本地改 → `git push` → 服务器 `git pull`”，不要直接在服务器上编辑文件，避免检出漂移。

### 🚀 已确认：自动部署（每 3 分钟）★重要

服务器 **root crontab** 里有两条自动拉取，两个检出**每 3 分钟各 `git pull` 一次**：

```cron
*/3 * * * * cd /app/telegram && git pull origin main >> /var/log/git-pull.log 2>&1
*/3 * * * * cd /var/www/telegram-repo && git pull >> /var/log/git-pull.log 2>&1
```

**所以发布内容其实只需一步：本地 `git push`，最多等 3 分钟两个站自动上线，完全不用登服务器。** 附录 A 里的“服务器 `git pull`”仅在你**想立刻生效**（不想等那几分钟）时才手动执行。

顺带记录该服务器其它相关 cron（非本文重点，供全局了解）：
- `investment_tracker/tracker.py` 定时生成 daily/weekly/monthly/rebalance 简报 → `/var/www/briefing`（对应 `brief.tgfootclub.com`）。
- `update-insurance-briefing.sh` 每周一 8:00 更新保险简报。
- `kol_radar/daily_cron.sh` 每日 0:30 更新 → `/var/www/kol-radar`。

排查自动部署是否正常：`tail /var/log/git-pull.log`；`git -C /app/telegram reflog -3`（能看到每次 cron 的 `pull ...: Fast-forward` 记录）。

---

## 二、美股学习站 `learn.tgfootclub.com`

### 2.1 形态

**整站就是一个文件**：`us-stock-learning.html`（约 1.3 MB，纯 HTML + 内联 CSS + 内联 JS，无外部依赖、无构建步骤）。双击就能本地预览，上传即上线。

### 2.2 内部结构（文件从上到下）

| 区块 | 位置（大致行号） | 作用 |
|------|------------------|------|
| `<head>` 内联 CSS | 顶部 | 全站样式、设计变量（`--primary` 等）、侧边栏/卡片/表格样式 |
| `curriculum` 数组 | **~286 行** | **课程目录数据源**：每节课一条元数据，驱动左侧导航 |
| `subgroupMap` / `subgroupLabels` | curriculum 后 | 课程的二级分组（子组）映射 |
| `renderNav()` | ~428 行 | 根据 `curriculum` 渲染左侧七大阶段导航 + 进度条 |
| `renderContent()` | ~516 行 | 渲染当前课正文（标题、时长、正文、完成按钮） |
| `getContent(id)` → `const C={...}` | **~1185 行** | **课程正文库**：`id → 正文 HTML 字符串` 的大字典 |
| 各类工具/播放器函数 | 535~800 行 | 退休规划器、章节音频、播客、投资大师最新动态 |
| 登录鉴权模块 | **~17000 行** | 付费墙：登录弹窗 + token 逻辑 |

### 2.3 课程数据模型（核心）

左侧导航完全由 `curriculum` 数组生成，每条形如：

```js
{id:'msft-valuation', phase:2, tag:'第9课', title:'微软估值：云+AI双引擎DCF实战', duration:'90 min'}
```

字段说明：

- `id`：唯一标识，同时是 URL 锚点（`#lesson-<id>`）和 `getContent` 里的 key。**必须唯一**。
- `phase`：阶段号 `0~7`。`0` 是「导读」，`1~7` 是七大学习阶段。
- `phaseLabel`：**只在某阶段的第一条**上写，作为该阶段标题（如 `'第二阶段：估值体系'`）。
- `tag`：课号徽标（`'第9课'` / `'📖 标的手册'` / `'🥚 彩蛋'` 等，可自由起）。
- `title`：完整标题。`shortTitle`：可选，侧边栏显示的短标题。
- `duration`：时长展示文本（`'90 min'` / `'自由阅读'`）。
- `type`：可选，仅 `'welcome'` 这类特殊页用。

七大阶段：1 市场基础 / 2 估值体系 / 3 投资工具 / 4 风险管理 / 5 养老规划 / 6 持续成长 / 7 香港金融职涯。

### 2.4 ✅ 新增一节课（标准操作）

只改 `us-stock-learning.html` 一个文件，两步：

**第 1 步**：在 `curriculum` 数组里加一行（放到希望它出现的阶段/位置）：

```js
{id:'my-new-lesson', phase:2, tag:'第42课', title:'新课完整标题', shortTitle:'新课短标题', duration:'60 min'},
```

- 若是**新开一个阶段**，在该阶段第一条上补 `phaseLabel:'第八阶段：xxx'`，并在 `renderNav()` 的 `phaseIcons`、`renderContent()` 的 `phaseNames`/`badgeMap` 里补对应图标/名称/配色（~431、527 行）。

**第 2 步**：在 `getContent` 的 `const C={...}` 里，加一个同名 key 的正文（用反引号模板字符串，内容是 HTML 片段，可用已有的 `<h2>`、`.table-wrap`、`.cards-grid`、`.card` 等现成样式类）：

```js
'my-new-lesson':`
<p>导语……</p>
<h2>小标题</h2>
<div class="table-wrap"><table>…</table></div>
`,
```

保存 → 按第一章「发布动作」上线。**不用动其它任何地方，导航、进度、锚点会自动生效。**

> 小抄：想快速找插入点，搜 `const curriculum=[`（加目录项）和 `function getContent(id){` 里的 `const C={`（加正文）。

### 2.5 付费墙 / 登录逻辑（改内容时基本不用碰）

- 登录接口：`COURSE_AUTH_API = '/api/v1/course/auth'`（~17006 行，同域相对路径）。
- token 存 `localStorage` 的 `us_stock_token`，有效期 8 小时；用户名/密码由 Telegram Bot 发放。
- URL 带 `?require_login=1` 且未登录时，进页面自动弹登录框。
- 音频、播客、投资大师最新动态分别打 `/api/v1/audio`、`/api/v1/podcast-audio`、`/api/v1/influencer-updates`——**这些是后端能力，加纯文字课程时无需关心；只有要接音频/动态时才需要后端配合**。

---

## 三、香港保险综合站 `insurance.tgfootclub.com`

> 注：`insurance-guide/` 当前被 `.cursorignore` 屏蔽（做上下文隔离用）。要在 Cursor 里直接编辑它时，把 `.cursorignore` 中对应的 `insurance-guide/` 行注释掉即可。本节结构依据 `INSURANCE_HUB.md`（整站总览，务必配合它看）。

### 3.1 形态

**多页静态 HTML 站**，入口是 `insurance-guide/index.html`（蓝色 Dashboard）。没有构建步骤，每个页面就是一个独立 HTML，靠首页/索引页的卡片链接互相跳转。

### 3.2 目录结构

```
insurance-guide/
├── index.html            🏠 综合 Dashboard 入口（对外首页）
├── guide.html            📖 完整版保险全指南
├── articles/             📚 100+ 篇专业文章（每篇一个 HTML）
│   └── xhs-publish/         小红书发布版
├── 30-questions.html     ❓ 港险投保 30 问
├── irr-calculator.html   📊 IRR 计算器（工具）
├── needs-assessment.html 🩺 家庭需求评估（工具）
├── dividend-rates.html   📈 分红实现率（数据）
├── wechat-copy/          公众号旧素材库
├── xhs-copy/  zhihu-md/  小红书 / 知乎旧素材库
├── operations/           📋 运营中心（对内）
│   ├── index.html          运营 Dashboard
│   ├── strategy/           战略文档
│   ├── published/          成稿仓库：wechat/ zhihu/ xhs/ pyq/
│   ├── assets/             PDF / Excel 客户资料
│   ├── tools/              convert_single.py（HTML→公众号/知乎转换器）
│   └── reference/          早期素材
├── fastapi_app/          保险简报 Web（FastAPI，独立部署，非主站）
└── insurance_briefing/   简报生成器（爬虫 + 分析，独立跑）
```

文章命名规律（`articles/` 内）：`cpa-NN-*`（CPA 旗舰研报）、`v22-xhsNN`（小红书 v2.2）、`mainland-xhs-NN` / `wx-NN` 等按渠道/系列区分。

### 3.3 页面之间的“逻辑”

- `index.html` 是唯一对外首页，用**卡片网格**把 4 大模块（核心知识库 / 内容工厂 / 运营战略 / 工具与资源）的入口铺出来。
- 文章不是自动索引的：**每个页面的链接是手写在 `index.html`（及各索引/hub 页）里的卡片**。所以新增文章 = 放文件 + 在索引页补一张卡片链接。
- `operations/` 是对内运营区，和对外首页平行；`fastapi_app` / `insurance_briefing` 是独立的“保险简报”后端，不属于主站静态页。

### 3.4 ✅ 新增内容（按类型）

**A. 新增一篇文章**
1. 把成稿 HTML 放进 `insurance-guide/articles/`，按命名规律取名（如 `cpa-15-xxx.html`）。
2. 在对应索引页（首页 `index.html` 或文章 hub 页）加一张卡片 `<a href="articles/cpa-15-xxx.html">…</a>`。
3. 发布（见第一章）。

**B. 新增公众号 / 知乎成稿**：放到 `operations/published/{wechat|zhihu}/`，命名 `公众号_<主题>.html` 或 `知乎_<主题>.md`。用 `operations/tools/convert_single.py` 可把 `articles/` 里的 HTML 一键转成公众号 HTML + 知乎 MD：

```bash
python insurance-guide/operations/tools/convert_single.py \
    "insurance-guide/articles/cpa-02-fulfillment-ratio.html" "港险分红实现率拆解"
```

**C. 新增战略文档**：放 `operations/strategy/`，会出现在运营中心的战略地图区。

**D. 改公众号导出样式**：编辑 `convert_single.py` 顶部的 `WECHAT_CSS`。

**E. 改首页/布局**：编辑 `insurance-guide/index.html`（或 `operations/index.html`）。

> 更详细的日常工作流见 `INSURANCE_HUB.md`（§日常工作流 / §维护建议）。

---

## 四、附：美股持仓复盘 · 文档中心（第三个站，自动生成型）

- 入口：`美股持仓复盘/站点/index.html`，由 `美股持仓复盘/tools/build_site.py` 生成。
- 逻辑：脚本扫描 `美股持仓复盘/` 下所有 `*.md`，按分组（核心文档/标的档案/操作手册/历史复盘/提示词模板/工具）打包成**一个自包含单页**（Markdown 以 base64 内嵌，前端用 marked.js 渲染）。
- **新增内容**：直接在 `美股持仓复盘/` 里加/改 `.md`，然后重跑 `python tools/build_site.py` 刷新站点即可（分组由 `build_site.py` 里的 `GROUP_ORDER` / `classify()` 控制）。
- 与前两个站不同：这个站是「先写 Markdown、再一键生成 HTML」，前两个是「直接改 HTML」。

---

## 五、速查表（要加东西时直接看这里）

| 我要做的事 | 改哪个文件 | 具体动作 |
|------------|-----------|----------|
| 学习站加一节课 | `us-stock-learning.html` | `curriculum` 加一条 + `getContent` 的 `C` 加同名正文 |
| 学习站加一个阶段 | `us-stock-learning.html` | 同上，首条加 `phaseLabel`，并补 `phaseIcons/phaseNames/badgeMap` |
| 学习站改样式 | `us-stock-learning.html` | 改 `<head>` 里的 CSS 变量/类 |
| 保险站加文章 | `insurance-guide/articles/` + `index.html` | 放 HTML + 首页/索引加卡片链接 |
| 保险站加成稿 | `operations/published/{渠道}/` | 放文件，或用 `convert_single.py` 生成 |
| 保险站改首页 | `insurance-guide/index.html` | 改卡片网格 |
| 复盘文档中心加内容 | `美股持仓复盘/*.md` | 加/改 md 后重跑 `tools/build_site.py` |
| 上线发布 | —— | 见第一章「发布动作」（提交推送 → 服务器更新） |

---

## 附录 A：部署命令（★已实测）

服务器：`ssh root@202.182.104.186`。nginx 站点配置文件：`/etc/nginx/sites-available/investment`（learn 与 insurance 都在此文件里；`sites-enabled/` 里以软链启用）。

### A1. 发布美股学习站（learn.tgfootclub.com）

```bash
# ① 本地：改完 us-stock-learning.html 后提交推送
git add us-stock-learning.html && git commit -m "learn: 新增/更新课程" && git push

# ② 服务器：拉取更新（检出目录 = /app/telegram）
ssh root@202.182.104.186
cd /app/telegram
git stash            # 先暂存服务器上的本地改动，避免 pull 冲突（见隐患说明）
git pull
# 若确认服务器本地改动不要了，也可强制对齐远程：
#   git fetch origin && git reset --hard origin/main
# 纯静态无需重载 nginx；改到登录后端才需要：
#   systemctl restart price-api
```

### A2. 发布香港保险站（insurance.tgfootclub.com）

```bash
# ① 本地：在 insurance-guide/ 里加/改文件后提交推送
git add insurance-guide/ && git commit -m "insurance: 新增文章/改首页" && git push

# ② 服务器：拉取更新（检出目录 = /var/www/telegram-repo，站点软链到它的 insurance-guide/）
ssh root@202.182.104.186
cd /var/www/telegram-repo
git pull
```

### A3. 常用检查 / 排错

```bash
nginx -t && systemctl reload nginx      # 改了 nginx 配置后校验并重载
systemctl status price-api              # 看登录后端状态；restart 同理
tail -f /var/log/nginx/error.log        # 看报错
readlink -f /var/www/hkins-guide        # 确认 insurance 软链指向（应为 /var/www/telegram-repo/insurance-guide）
```

- **缓存**：nginx 对 `.html/.css/.js` 设了 `expires 7d`。刚发布看不到更新时，浏览器**硬刷新**（Mac：Cmd+Shift+R），或对单文件加查询串 `?v=日期` 破缓存。
- **两个检出别搞混**：learn 只认 `/app/telegram`，insurance 只认 `/var/www/telegram-repo`。若改动同时涉及两站，两个目录都要 `git pull`。

*最后更新：2026-08-02（部署信息已通过 SSH 实测核实）*
